name: Daily Database Reset

on:
  schedule:
    # Run at 2:00 AM UTC daily (adjust timezone as needed)
    - cron: '0 2 * * *'
  workflow_dispatch:
    # Allow manual trigger for testing

jobs:
  reset-preview:
    name: Reset Preview Database
    runs-on: ubuntu-latest
    steps:
      - name: Reset Preview Database
        run: |
          echo "🔄 Resetting Preview database..."
          # Try the preview URL with proper error handling
          RESPONSE=$(curl -s -w "HTTPSTATUS:%{http_code}" -X POST "https://react-demo-preview.vercel.app/api/admin/reset" \
            -H "Content-Type: application/json")
          HTTP_CODE=$(echo $RESPONSE | tr -d '\n' | sed -e 's/.*HTTPSTATUS://')
          BODY=$(echo $RESPONSE | sed -e 's/HTTPSTATUS\:.*//g')

          if [ "$HTTP_CODE" -eq 200 ] || [ "$HTTP_CODE" -eq 201 ]; then
            echo "✅ Preview database reset successful!"
            echo "📊 Response: $BODY"
          else
            echo "⚠️ Preview reset returned HTTP $HTTP_CODE (database may be in memory mode)"
            echo "📄 Response: $BODY"
            # Don't fail the job for preview, as it might be in memory mode
          fi

  reset-production:
    name: Reset Production Database
    runs-on: ubuntu-latest
    needs: reset-preview
    steps:
      - name: Reset Production Database
        run: |
          echo "🔄 Resetting Production database..."
          # Reset production database with comprehensive error handling
          RESPONSE=$(curl -s -w "HTTPSTATUS:%{http_code}" -X POST "https://react-demo.vercel.app/api/admin/reset" \
            -H "Content-Type: application/json")
          HTTP_CODE=$(echo $RESPONSE | tr -d '\n' | sed -e 's/.*HTTPSTATUS://')
          BODY=$(echo $RESPONSE | sed -e 's/HTTPSTATUS\:.*//g')

          echo "🔍 Debug Info:"
          echo "   HTTP Code: $HTTP_CODE"
          echo "   Response Body: $BODY"

          if [ "$HTTP_CODE" -eq 200 ] || [ "$HTTP_CODE" -eq 201 ] || [ "$HTTP_CODE" -eq 204 ]; then
            echo "✅ Production database reset successful!"
            echo "📊 Response: $BODY"
          elif [ "$HTTP_CODE" -eq 404 ]; then
            echo "⚠️ Production reset endpoint not found (HTTP 404)"
            echo "💡 This might indicate the API is not properly deployed or the URL is incorrect"
            echo "📄 Response: $BODY"
            # Don't fail - might be a temporary deployment issue
          elif [ "$HTTP_CODE" -eq 500 ]; then
            echo "⚠️ Production reset had a server error (HTTP 500)"
            echo "� The reset might have worked despite the error"
            echo "�📄 Response: $BODY"
            # Don't fail - server errors don't necessarily mean the reset failed
          else
            echo "⚠️ Production reset returned HTTP $HTTP_CODE"
            echo "💡 This might still indicate success depending on your API design"
            echo "📄 Response: $BODY"
            # Don't fail automatically - let's see what's actually happening first
          fi

  notify:
    name: Notify Results
    runs-on: ubuntu-latest
    needs: [reset-preview, reset-production]
    if: always()
    steps:
      - name: Summary
        run: |
          echo "📊 Daily Database Reset Summary"
          echo "Preview: ${{ needs.reset-preview.result }}"
          echo "Production: ${{ needs.reset-production.result }}"
