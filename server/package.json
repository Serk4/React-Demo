{
  "name": "react-demo-server",
  "version": "1.0.0",
  "description": "Backend API for React Demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "db:reset": "node scripts/simple-reset.js --verbose",
    "db:reset:local": "node scripts/daily-reset.js",
    "db:reset:demo": "node scripts/simple-reset.js --verbose"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
